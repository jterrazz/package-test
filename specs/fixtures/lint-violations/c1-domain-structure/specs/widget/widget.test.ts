import { expect, test } from 'vitest';

test('builds the widget', () => {
    // Given - a widget at the facet root, not inside a domain
    // Then - the placement itself is the C1 violation
    expect(1).toBe(1);
});
