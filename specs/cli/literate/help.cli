# The plainest literate spec: no ground to state, one command, one golden.
test: prints the command list when asked for help
given: an empty working directory
then: every command the binary offers is listed once

$ help
exit: 0
Usage: cli <command>

Commands:
  build   Build the project
  check   Check code quality
