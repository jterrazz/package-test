test: a header names an environment set registered in code
given: the frozen env set, plus one inline pair the header adds
then: both reach the child process
env: frozen EXTRA=inline

$ env
exit: 0
MY_VAR=from-the-frozen-set
HOME={{any}}
EXTRA=inline
DB_URL=unset
DATABASE_URL=unset
REDIS_URL=unset
