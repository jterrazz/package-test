# A nested spec tree

`@fixture/clean` keeps its facet trees under `web/`, the shape
`@jterrazz/typescript`'s `discover_specs_roots` names. The path-less run has to
find this root, or its findings can never reach the ratchet.
