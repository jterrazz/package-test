import { expect, test } from 'vitest';

test('creates a user', () => {
    // Given - a `.spec.ts` that never reached a `specs/` tree
    // Then - C12's first clause names it: the suffix says the assembled product
    expect(1).toBe(1);
});
