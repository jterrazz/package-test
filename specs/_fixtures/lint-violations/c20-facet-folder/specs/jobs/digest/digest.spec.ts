import { expect, test } from 'vitest';

test('sends the digest', () => {
    // Given - nothing constructs the runner this folder promises
    const sent = true;
    // Then - the folder is a facet in name only
    expect(sent).toBe(true);
});
