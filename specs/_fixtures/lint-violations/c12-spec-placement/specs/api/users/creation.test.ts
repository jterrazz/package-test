import { expect, test } from 'vitest';

test('creates a user', () => {
    // Given - a spec of the assembled product wearing the unit's suffix
    // Then - C12 renames it
    expect(1).toBe(1);
});
