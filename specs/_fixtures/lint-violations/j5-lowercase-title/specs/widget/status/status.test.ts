import { expect, test } from 'vitest';

test('Builds the widget', () => {
    // Given - a test whose title starts uppercase
    // Then - the title itself is the J5 violation
    expect(1).toBe(1);
});
