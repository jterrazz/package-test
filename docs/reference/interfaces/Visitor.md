<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: Visitor

Defined in: [core/ports/browser.port.ts:88](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L88)

The visitor — the interaction vocabulary handed to a visit scenario.
Every action auto-waits (playwright actionability); `see()` is the single
synchronization primitive: it retries until the element is visible and
fails at the timeout. There is no sleep and no conditional helper.

## Properties

### check

```ts
check: (element) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:90](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L90)

Check a checkbox or radio.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>

***

### click

```ts
click: (element) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:92](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L92)

Click the element.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>

***

### fill

```ts
fill: (element, value) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:94](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L94)

Fill a form field with a value.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |
| `value` | `string` |

#### Returns

`Promise`\<`void`\>

***

### goto

```ts
goto: (path) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:96](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L96)

Navigate to a path of the site under test.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |

#### Returns

`Promise`\<`void`\>

***

### hover

```ts
hover: (element) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:98](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L98)

Hover the element.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>

***

### press

```ts
press: (key) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:100](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L100)

Press a key (e.g. `Enter`).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |

#### Returns

`Promise`\<`void`\>

***

### see

```ts
see: (element) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:102](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L102)

Wait until the element is visible — the only synchronization primitive.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>

***

### select

```ts
select: (element, option) => Promise<void>;
```

Defined in: [core/ports/browser.port.ts:104](https://github.com/jterrazz/package-test/blob/main/src/core/ports/browser.port.ts#L104)

Select an option in a select field.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |
| `option` | `string` |

#### Returns

`Promise`\<`void`\>
