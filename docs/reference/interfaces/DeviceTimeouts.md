<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: DeviceTimeouts

Defined in: [core/ports/device.port.ts:104](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L104)

How long a device session waits, in milliseconds. Every field is optional
and falls back to the framework's own default, so a runner states only the
wait it needs to move — the defaults suit a release build, and a project
that drives a DEV build pays for its bundler's cold boot.

## Properties

### action?

```ts
optional action?: number;
```

Defined in: [core/ports/device.port.ts:109](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L109)

How long every verb polls for a visible match before refusing.
Default: `30_000` — enough for a cold app boot on a release bundle.

***

### launch?

```ts
optional launch?: number;
```

Defined in: [core/ports/device.port.ts:114](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L114)

How long the FIRST session waits for WebDriverAgent to build and
launch on the simulator. Default: `240_000`.
