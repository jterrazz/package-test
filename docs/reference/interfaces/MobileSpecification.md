<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: MobileSpecification

Defined in: [core/specification/shared/builder.ts:269](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L269)

The `mobile` facet — screen chain entry handed out by
`specification.mobile()`. `.open()` is the single, terminal action: it
terminates and relaunches the app (deterministic fresh state), applies the
deep link, runs the scenario, and captures the final screen.

## Properties

### intercept

```ts
intercept: (file) => MobileSpecification;
```

Defined in: [core/specification/shared/builder.ts:275](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L275)

Declare the chain's backend exchanges from an `intercepts/<name>.http`
file — served by the declared stub backend (requires the runner's
`backend` option). Multiple calls layer in order.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `file` | `string` |

#### Returns

`MobileSpecification`

***

### open

```ts
open: (deepLink?, scenario?) => Promise<ScreenResult>;
```

Defined in: [core/specification/shared/builder.ts:282](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L282)

Relaunch the app and resolve with the captured screen. With a deep
link, the app opens on it; with a scenario, the visitor interacts
first (the When) and the capture reflects the FINAL screen state.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `deepLink?` | `string` |
| `scenario?` | [`MobileScenario`](../type-aliases/MobileScenario.md) |

#### Returns

`Promise`\<[`ScreenResult`](../classes/ScreenResult.md)\>
