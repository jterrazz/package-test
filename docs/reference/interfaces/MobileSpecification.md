<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: MobileSpecification

Defined in: [core/specification/shared/builder.ts:290](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L290)

The `mobile` facet — screen chain entry handed out by
`specification.mobile()`. `.open()` is the single, terminal action: it
terminates and relaunches the app (deterministic fresh state), applies the
deep link, runs the scenario, and captures the final screen.

## Properties

### intercept

```ts
intercept: InterceptMethod<MobileSpecification>;
```

Defined in: [core/specification/shared/builder.ts:295](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L295)

Declare the chain's backend contracts — served by the declared stub
backend (requires the runner's `backend` option). Multiple calls append.

***

### open

```ts
open: (deepLink?, scenario?) => Promise<ScreenResult>;
```

Defined in: [core/specification/shared/builder.ts:302](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L302)

Relaunch the app and resolve with the captured screen. With a deep
link, the app opens on it; with a scenario, the visitor interacts
first (the When) and the capture reflects the FINAL screen state.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `deepLink?` | `string` |
| `scenario?` | [`MobileScenario`](../type-aliases/MobileScenario.md) |

#### Returns

`Promise`\<[`ScreenResult`](../classes/ScreenResult.md)\>
