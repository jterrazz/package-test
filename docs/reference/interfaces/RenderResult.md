<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: RenderResult

Defined in: [src/specification/facets/component/component.result.ts:12](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L12)

What a render resolves to — the component as the page saw it, after the
scenario ran.

Five accessors and no more: the ARIA outline a golden pins, the two
renderings a probe reads, and the two streams a spec wants silent. Nothing
here reaches back into React — a spec that needs the component's internals
is asserting the implementation, not the surface a user meets.

## Properties

### console

```ts
readonly console: RenderedText;
```

Defined in: [src/specification/facets/component/component.result.ts:14](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L14)

The browser console as a stream — one `[type] text` line per message.

***

### content

```ts
readonly content: RenderedText;
```

Defined in: [src/specification/facets/component/component.result.ts:16](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L16)

Rendered text of the whole document body (`innerText`).

***

### errors

```ts
readonly errors: RenderedText;
```

Defined in: [src/specification/facets/component/component.result.ts:18](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L18)

Console messages of type `error` only, plus any uncaught page error.

***

### html

```ts
readonly html: RenderedText;
```

Defined in: [src/specification/facets/component/component.result.ts:20](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L20)

The mounted markup, serialised — the escape hatch for a class or an attribute.

***

### tree

```ts
readonly tree: RenderedText;
```

Defined in: [src/specification/facets/component/component.result.ts:22](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/component/component.result.ts#L22)

The ARIA snapshot of the rendered body — the outline a golden pins.
