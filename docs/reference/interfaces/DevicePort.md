<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: DevicePort

Defined in: [core/ports/device.port.ts:104](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L104)

Abstract device interface for the mobile specification runner.
One implementation lives in `integrations/appium/` — a single driver
session per runner, created on the first `open()` and reused; each open
terminates and relaunches the app for a deterministic fresh state.

## Properties

### close

```ts
close: () => Promise<void>;
```

Defined in: [core/ports/device.port.ts:106](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L106)

End the driver session (idempotent).

#### Returns

`Promise`\<`void`\>

***

### open

```ts
open: (options) => Promise<DeviceScreen>;
```

Defined in: [core/ports/device.port.ts:108](https://github.com/jterrazz/package-test/blob/main/src/core/ports/device.port.ts#L108)

Relaunch the app, apply the deep link, run the scenario, capture the final screen.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`DeviceOpenOptions`](DeviceOpenOptions.md) |

#### Returns

`Promise`\<[`DeviceScreen`](DeviceScreen.md)\>
