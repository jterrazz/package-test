<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: CliSpecification\<DatabaseKey\>

Defined in: [core/specification/shared/builder.ts:226](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L226)

The `cli` facet — command chain entry handed out by `specification.cli()`.
Setup methods chain; `.exec()` is the single terminal action (CONVENTIONS
B2) — `{ waitFor?, timeout? }` covers long-running processes.

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `DatabaseKey` *extends* `string` | `string` |

## Properties

### env

```ts
env: (env) => CliSpecification<DatabaseKey>;
```

Defined in: [core/specification/shared/builder.ts:228](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L228)

Set environment variables on the child process. `$WORKDIR` expands; `null` unsets.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `env` | [`CliEnv`](../type-aliases/CliEnv.md) |

#### Returns

`CliSpecification`\<`DatabaseKey`\>

***

### exec

```ts
exec: (args?, options?) => Promise<CliResult>;
```

Defined in: [core/specification/shared/builder.ts:247](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L247)

Execute the command (or sequence of commands) and resolve with the
result. Called with no arguments (`cli.exec()`), the binary runs bare —
no CLI arguments. With `{ waitFor, timeout }`, the process is
long-running: it resolves when the pattern appears and is killed at the
timeout.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `args?` | `string` \| `string`[] |
| `options?` | [`ExecOptions`](ExecOptions.md) |

#### Returns

`Promise`\<[`CliResult`](../classes/CliResult.md)\>

***

### fixture

```ts
fixture: (path) => CliSpecification<DatabaseKey>;
```

Defined in: [core/specification/shared/builder.ts:236](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L236)

Copy a fixture into the working directory before execution. The path is
feature-local (`<test-dir>/fixtures/<path>`) or, with a `$FIXTURES/`
prefix, from the shared pool at `<specs-root>/fixtures/`. A trailing
slash spreads a directory's contents into the cwd; without one a
directory (or file) is copied under its own name. Chained calls layer.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |

#### Returns

`CliSpecification`\<`DatabaseKey`\>

***

### run

```ts
run: (file, options?) => Promise<CliResult>;
```

Defined in: [core/specification/shared/builder.ts:255](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L255)

Run a `<case>.spec.yaml` document — its ground (fixtures, env sets,
servers) and every run, each asserted — and resolve with the LAST run's
result, so a `.test.ts` can add an assertion the document cannot express
(a directory golden, a grep). The path is relative to the test file's own
directory, where the document lives.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `file` | `string` |
| `options?` | [`LiterateRunFlags`](LiterateRunFlags.md) |

#### Returns

`Promise`\<[`CliResult`](../classes/CliResult.md)\>

***

### seed

```ts
seed: (file, options?) => CliSpecification<DatabaseKey>;
```

Defined in: [core/specification/shared/builder.ts:238](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L238)

Queue a SQL seed file from `_seeds/` to run against a database before the action.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `file` | `string` |
| `options?` | \{ `database?`: `DatabaseKey`; \} |
| `options.database?` | `DatabaseKey` |

#### Returns

`CliSpecification`\<`DatabaseKey`\>
