<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: CliSpecificationOptions\<Services\>

Defined in: [core/specification/cli/start-cli.ts:26](https://github.com/jterrazz/package-test/blob/main/src/core/specification/cli/start-cli.ts#L26)

Options for [specification.cli](../variables/specification.md#property-cli).

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Services` *extends* [`ServiceRecord`](../type-aliases/ServiceRecord.md) | [`ServiceRecord`](../type-aliases/ServiceRecord.md) |

## Properties

### docker?

```ts
optional docker?: DockerSpecConfig;
```

Defined in: [core/specification/cli/start-cli.ts:34](https://github.com/jterrazz/package-test/blob/main/src/core/specification/cli/start-cli.ts#L34)

Opt-in Docker awareness. When set, every spec generates a unique
test-run id, injects it into the child process env under `envVar`,
and exposes `.container(name)` accessors on the result that lazily
query Docker. Always declare results with `await using` so leaked
containers get force-removed at scope exit (CONVENTIONS B5).

***

### root?

```ts
optional root?: string;
```

Defined in: [core/specification/cli/start-cli.ts:41](https://github.com/jterrazz/package-test/blob/main/src/core/specification/cli/start-cli.ts#L41)

Project-root override (CONVENTIONS A9) — the single meaning of `root`:
it anchors compose detection and local-bin resolution for the tested
binary. It is NOT a fixtures root; `.fixture()` resolves feature-local
or `$FIXTURES/` paths on its own.

***

### services?

```ts
optional services?: Services;
```

Defined in: [core/specification/cli/start-cli.ts:48](https://github.com/jterrazz/package-test/blob/main/src/core/specification/cli/start-cli.ts#L48)

Named infrastructure record started via testcontainers. Connection
URLs are injected automatically into the child env: `<KEY>_URL` per
service, plus `DATABASE_URL` / `REDIS_URL` when unambiguous
(CONVENTIONS B6). `.env()` overrides.

***

### transform?

```ts
optional transform?: (text) => string;
```

Defined in: [core/specification/cli/start-cli.ts:55](https://github.com/jterrazz/package-test/blob/main/src/core/specification/cli/start-cli.ts#L55)

Escape hatch: normaliser applied to result.stdout / result.stderr
before every comparison, AFTER the default ANSI strip (CONVENTIONS
D6). Does NOT mutate the raw `.text` accessor. Prefer `{{token}}`
placeholders in fixtures.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `text` | `string` |

#### Returns

`string`
