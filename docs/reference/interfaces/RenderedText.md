<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: RenderedText

Defined in: [src/facets/component/rendered-text.ts:12](https://github.com/jterrazz/package-test/blob/main/src/facets/component/rendered-text.ts#L12)

A stream captured INSIDE the page.

Structurally identical to any other [TextAccessor](TextAccessor.md) — the same
`{{token}}` grammar, the same ANSI stripping, the same `.grep()` — and
marked so the matcher augmentation can say the one thing that differs: its
golden lives on the server, so `toMatch` is awaited here where it is
synchronous everywhere else.

## Extends

- [`TextAccessor`](TextAccessor.md)

## Properties

### capturedInPage

```ts
readonly capturedInPage: true;
```

Defined in: [src/facets/component/rendered-text.ts:14](https://github.com/jterrazz/package-test/blob/main/src/facets/component/rendered-text.ts#L14)

The marker the `toMatch` overload reads. Never asserted on directly.

***

### text

```ts
readonly text: string;
```

Defined in: [src/model/result/text.ts:43](https://github.com/jterrazz/package-test/blob/main/src/model/result/text.ts#L43)

The raw captured text (never transformed, ANSI preserved).

#### Inherited from

[`TextAccessor`](TextAccessor.md).[`text`](TextAccessor.md#text)

## Methods

### grep()

```ts
grep(pattern): TextAccessor;
```

Defined in: [src/model/result/text.ts:76](https://github.com/jterrazz/package-test/blob/main/src/model/result/text.ts#L76)

Keep only the blank-line-separated blocks of the text that contain
`pattern` (how linter/compiler output is structured), returned as a new
`TextAccessor` — the same subject type, so the result is chainable and
snapshot-able. The scalpel for probing large tool outputs; snapshot the
whole surface by default, reach for `.grep()` for targeted checks.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pattern` | `string` |

#### Returns

[`TextAccessor`](TextAccessor.md)

#### Inherited from

[`TextAccessor`](TextAccessor.md).[`grep`](TextAccessor.md#grep)

***

### toString()

```ts
toString(): string;
```

Defined in: [src/model/result/text.ts:83](https://github.com/jterrazz/package-test/blob/main/src/model/result/text.ts#L83)

#### Returns

`string`

#### Inherited from

[`TextAccessor`](TextAccessor.md).[`toString`](TextAccessor.md#tostring)

***

### valueOf()

```ts
valueOf(): string;
```

Defined in: [src/model/result/text.ts:87](https://github.com/jterrazz/package-test/blob/main/src/model/result/text.ts#L87)

#### Returns

`string`

#### Inherited from

[`TextAccessor`](TextAccessor.md).[`valueOf`](TextAccessor.md#valueof)
