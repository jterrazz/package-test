<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: MobileVisitor

Defined in: core/ports/device.port.ts:48

The visitor — the interaction vocabulary handed to a mobile scenario.
Every verb auto-waits by polling until at least one visible match exists;
`see()` is the single synchronization primitive: it retries until the
element is visible and fails at the timeout. There is no sleep and no
conditional helper.

## Properties

### fill

```ts
fill: (element, value) => Promise<void>;
```

Defined in: core/ports/device.port.ts:50

Fill a text field with a value.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |
| `value` | `string` |

#### Returns

`Promise`\<`void`\>

***

### see

```ts
see: (element) => Promise<void>;
```

Defined in: core/ports/device.port.ts:52

Wait until the element is visible — the only synchronization primitive.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>

***

### tap

```ts
tap: (element) => Promise<void>;
```

Defined in: core/ports/device.port.ts:54

Tap the element.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](ElementRef.md) |

#### Returns

`Promise`\<`void`\>
