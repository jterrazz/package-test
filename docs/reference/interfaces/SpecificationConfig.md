<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: SpecificationConfig

Defined in: [core/specification/shared/builder.ts:64](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L64)

Adapter configuration passed to the specification facets at setup time.

## Properties

### backend?

```ts
optional backend?: StubBackend;
```

Defined in: [core/specification/shared/builder.ts:69](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L69)

The declared stub backend (website/mobile facets) — armed with the
chain's contracts before every terminal action.

***

### backendUrl?

```ts
optional backendUrl?: string;
```

Defined in: [core/specification/shared/builder.ts:74](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L74)

Base URL of the running stub backend — allow-listed through the
browser's `external: 'block'` policy so client-side fetches reach it.

***

### baseUrl?

```ts
optional baseUrl?: string;
```

Defined in: [core/specification/shared/builder.ts:76](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L76)

Base URL of the website under test (website facet only).

***

### browser?

```ts
optional browser?: () => Promise<BrowserPort>;
```

Defined in: [core/specification/shared/builder.ts:82](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L82)

Lazy browser accessor (website facet only). The first `.visit()`
launches the shared browser instance; `.fetch()`-only spec files never
pay the browser cost.

#### Returns

`Promise`\<[`BrowserPort`](BrowserPort.md)\>

***

### bundleId?

```ts
optional bundleId?: string;
```

Defined in: [core/specification/shared/builder.ts:90](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L90)

Bundle id of the app under test (mobile facet only) — the app `.open()` relaunches.

***

### command?

```ts
optional command?: CliPort;
```

Defined in: [core/specification/shared/builder.ts:91](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L91)

***

### database?

```ts
optional database?: DatabasePort;
```

Defined in: [core/specification/shared/builder.ts:92](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L92)

***

### databaseKeys?

```ts
optional databaseKeys?: string[];
```

Defined in: [core/specification/shared/builder.ts:98](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L98)

Keys of the declared services record that are databases. Drives the
CONVENTIONS A7 rule: with 2+ databases the `database` option is
mandatory on `.seed()` / `.table()`; with exactly one it is forbidden.

***

### databases?

```ts
optional databases?: Map<string, DatabasePort>;
```

Defined in: [core/specification/shared/builder.ts:99](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L99)

***

### device?

```ts
optional device?: () => Promise<DevicePort>;
```

Defined in: [core/specification/shared/builder.ts:106](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L106)

Lazy device accessor (mobile facet only). The first `.open()` creates
the shared driver session; the appium/webdriverio integration stays a
lazy import so the optional peer is only loaded when a spec opens the
app.

#### Returns

`Promise`\<[`DevicePort`](DevicePort.md)\>

***

### dockerConfig?

```ts
optional dockerConfig?: DockerSpecConfig;
```

Defined in: [core/specification/shared/builder.ts:107](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L107)

***

### dockerTestRunId?

```ts
optional dockerTestRunId?: string;
```

Defined in: [core/specification/shared/builder.ts:114](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L114)

Unique id shared by every spec from this runner instance.
Stable for the runner's lifetime so multi-step tests (spawn in
one run, inspect in another) see the same container scope. The
facet factories auto-populate this when `dockerConfig` is present.

***

### envSets?

```ts
optional envSets?: Record<string, CliEnv>;
```

Defined in: [core/specification/shared/builder.ts:119](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L119)

Named environment SETS a spec document may name by bare word
(`env: frozen`). Declared once per app in `specification.cli()`.

***

### external?

```ts
optional external?: "allow" | "block";
```

Defined in: [core/specification/shared/builder.ts:88](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L88)

Cross-origin request policy for visits (website facet only): `'block'`
aborts requests leaving the site under test — the browser-side analog
of strict intercepts.

***

### interceptDisabledReason?

```ts
optional interceptDisabledReason?: string;
```

Defined in: [core/specification/shared/builder.ts:124](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L124)

When set, `.intercept()` is unavailable on this runner and throws this
reason immediately (compose mode — MSW is in-process, CONVENTIONS I3).

***

### jobs?

```ts
optional jobs?: JobHandle[];
```

Defined in: [core/specification/shared/builder.ts:125](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L125)

***

### root?

```ts
optional root?: string;
```

Defined in: [core/specification/shared/builder.ts:127](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L127)

The project root — the working directory a document's `serve:` command runs from.

***

### server?

```ts
optional server?: ServerPort;
```

Defined in: [core/specification/shared/builder.ts:128](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L128)

***

### serveRegistry?

```ts
optional serveRegistry?: Record<string, LiterateServeRegistration>;
```

Defined in: [core/specification/shared/builder.ts:133](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L133)

Named servers a spec document may start (`serve: [mcp]`). Declared once
per app in `specification.cli()`.

***

### services?

```ts
optional services?: Record<string, ServiceHandle>;
```

Defined in: [core/specification/shared/builder.ts:140](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L140)

The declared services record. In cli mode, drives the automatic
connection-URL injection into the child env (CONVENTIONS B6):
`<KEY>_URL` per service, plus `DATABASE_URL` / `REDIS_URL` when
unambiguous.

***

### transform?

```ts
optional transform?: (text) => string;
```

Defined in: [core/specification/shared/builder.ts:145](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L145)

Optional normaliser applied to command stdout/stderr before every
comparison. Does not mutate the raw `.text` accessor.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `text` | `string` |

#### Returns

`string`
