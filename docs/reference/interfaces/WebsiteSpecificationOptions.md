<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: WebsiteSpecificationOptions

Defined in: core/specification/website/start-website.ts:15

Options for [specification.website](../variables/specification.md#property-website).

## Properties

### external?

```ts
optional external?: "allow" | "block";
```

Defined in: core/specification/website/start-website.ts:21

Cross-origin request policy for visits. Default: `'block'` with a
local `server` (deterministic — analytics and CDNs never leave the
machine), `'allow'` with a deployed `url`.

***

### root?

```ts
optional root?: string;
```

Defined in: core/specification/website/start-website.ts:26

Project-root override (CONVENTIONS A9): the working directory of the
`server` command. Auto-discovered from the calling file when absent.

***

### server?

```ts
optional server?: ServeOptions;
```

Defined in: core/specification/website/start-website.ts:32

Start the site locally: a shell command receiving a free port as
`PORT`, polled on `ready` (default `/`) until it answers HTTP.
Exactly one of `server` / `url` is required.

***

### url?

```ts
optional url?: string;
```

Defined in: core/specification/website/start-website.ts:37

Target an already-running site (a deployed URL, a preview URL, a dev
server). Exactly one of `server` / `url` is required.
