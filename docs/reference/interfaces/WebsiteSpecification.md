<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: WebsiteSpecification

Defined in: [core/specification/shared/builder.ts:264](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L264)

The `website` facet — page chain entry handed out by
`specification.website()`. Setup methods chain; action methods are
terminal. `.visit()` renders the page in the shared browser; `.fetch()`
performs one raw HTTP exchange and never follows redirects.

## Properties

### fetch

```ts
fetch: (path) => Promise<FetchResult>;
```

Defined in: [core/specification/shared/builder.ts:274](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L274)

Perform one raw HTTP GET — redirects surface as 3xx results, never followed.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |

#### Returns

`Promise`\<[`FetchResult`](../classes/FetchResult.md)\>

***

### headers

```ts
headers: (headers) => WebsiteSpecification;
```

Defined in: [core/specification/shared/builder.ts:266](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L266)

Set HTTP headers for the exchange (incl. User-Agent overrides). Multiple calls merge.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `headers` | `Record`\<`string`, `string`\> |

#### Returns

`WebsiteSpecification`

***

### intercept

```ts
intercept: InterceptMethod<WebsiteSpecification>;
```

Defined in: [core/specification/shared/builder.ts:271](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L271)

Declare the chain's backend contracts — served by the declared stub
backend (requires the runner's `backend` option). Multiple calls append.

***

### visit

```ts
visit: (path, scenario?) => Promise<PageResult>;
```

Defined in: [core/specification/shared/builder.ts:280](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/builder.ts#L280)

Render the page in the shared browser and resolve with the captured
document. With a scenario, the visitor interacts first (the When) and
the capture reflects the FINAL page state.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |
| `scenario?` | [`VisitScenario`](../type-aliases/VisitScenario.md) |

#### Returns

`Promise`\<[`PageResult`](../classes/PageResult.md)\>
