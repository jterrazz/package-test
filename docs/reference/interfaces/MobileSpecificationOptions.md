<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: MobileSpecificationOptions

Defined in: [core/specification/mobile/start-mobile.ts:16](https://github.com/jterrazz/package-test/blob/main/src/core/specification/mobile/start-mobile.ts#L16)

Options for [specification.mobile](../variables/specification.md#property-mobile).

## Properties

### app

```ts
app: object;
```

Defined in: [core/specification/mobile/start-mobile.ts:18](https://github.com/jterrazz/package-test/blob/main/src/core/specification/mobile/start-mobile.ts#L18)

The app under test — terminated and relaunched by every `.open()`.

#### bundleId

```ts
bundleId: string;
```

Bundle id of the installed app (`com.example.app`).

***

### device

```ts
device: object;
```

Defined in: [core/specification/mobile/start-mobile.ts:23](https://github.com/jterrazz/package-test/blob/main/src/core/specification/mobile/start-mobile.ts#L23)

The simulator to run on — resolved through `xcrun simctl`.

#### name

```ts
name: string;
```

Simulator name, matched exactly (`iPhone 17`).

#### os?

```ts
optional os?: string;
```

OS narrowing when the name exists on several runtimes — `'26.5'` or `'iOS 26.5'`.

#### udid?

```ts
optional udid?: string;
```

Explicit UDID — skips name/os resolution entirely.

***

### root?

```ts
optional root?: string;
```

Defined in: [core/specification/mobile/start-mobile.ts:36](https://github.com/jterrazz/package-test/blob/main/src/core/specification/mobile/start-mobile.ts#L36)

Project-root override (CONVENTIONS A9): where the appium binary is
resolved from (`node_modules/.bin`). Auto-discovered from the calling
file when absent.
