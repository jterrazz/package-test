<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Interface: ServeOptions

Defined in: [core/specification/website/serve.adapter.ts:10](https://github.com/jterrazz/package-test/blob/main/src/core/specification/website/serve.adapter.ts#L10)

Options for a local server started by the framework.

## Properties

### command

```ts
command: string;
```

Defined in: [core/specification/website/serve.adapter.ts:12](https://github.com/jterrazz/package-test/blob/main/src/core/specification/website/serve.adapter.ts#L12)

Shell command that starts the site. Receives the chosen port as `PORT`.

***

### port?

```ts
optional port?: number;
```

Defined in: [core/specification/website/serve.adapter.ts:14](https://github.com/jterrazz/package-test/blob/main/src/core/specification/website/serve.adapter.ts#L14)

Fixed port. Default: an OS-assigned free port, injected as `PORT`.

***

### ready?

```ts
optional ready?: string | RegExp;
```

Defined in: [core/specification/website/serve.adapter.ts:26](https://github.com/jterrazz/package-test/blob/main/src/core/specification/website/serve.adapter.ts#L26)

How readiness is observed. Two forms:

- a **path** (or nothing) — the framework picks the port, injects it as
  `PORT`, and polls `<baseUrl><path>` until it answers any HTTP status
  (default `/`). This is `specification.website()`'s form.
- a **regex over the child's output** with ONE capture group holding the
  port the server chose itself. Nothing is injected as `PORT`: the server
  announces its port and the framework reads it from the banner. This is
  the form a literate `serve:` registration uses.

***

### timeout?

```ts
optional timeout?: number;
```

Defined in: [core/specification/website/serve.adapter.ts:28](https://github.com/jterrazz/package-test/blob/main/src/core/specification/website/serve.adapter.ts#L28)

Readiness budget in milliseconds. Default 30 000.
