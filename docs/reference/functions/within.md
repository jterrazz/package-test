<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Function: within()

```ts
function within(scope, target): ElementRef;
```

Defined in: [specification/facets/website/elements.ts:109](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/website/elements.ts#L109)

Restrict a descriptor to the inside of another — the answer to ambiguity,
and the one the framework prefers over a test id.

    click(within(navigation(), link('Articles')))

Composes outside-in: the scope may itself be scoped, so a deeply nested
target reads `within(main(), within(region('Series'), link('Part 2')))`.
Any descriptor works as a scope, including `testId()` when a container has
no landmark role to stand on.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `scope` | [`ElementRef`](../type-aliases/ElementRef.md) |
| `target` | [`ElementRef`](../type-aliases/ElementRef.md) |

## Returns

[`ElementRef`](../type-aliases/ElementRef.md)
