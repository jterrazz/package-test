<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Function: valued()

```ts
function valued(element, value): ElementRef;
```

Defined in: [src/specification/facets/website/elements.ts:195](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/website/elements.ts#L195)

What the field holds: `see(valued(field('Title'), 'Launch teaser'))`, and
`gone(valued(field('Title'), '…'))` for a value it no longer holds.

A modifier for the same reason as [disabled](disabled.md): what a field holds is a
state of an element the vocabulary already names, and this is how the ONE
field a test is about is waited for — a golden pins a whole outline instead.
Read from the node's live value, never from the `value` ATTRIBUTE, which a
controlled input never moves.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](../type-aliases/ElementRef.md) |
| `value` | `string` |

## Returns

[`ElementRef`](../type-aliases/ElementRef.md)
