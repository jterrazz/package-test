<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Function: sqlite()

```ts
function sqlite(options?): SqliteHandle;
```

Defined in: src/integrations/sqlite/sqlite.ts:457

Create a SQLite service handle. Uses file-copy isolation for parallel tests.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | [`SqliteOptions`](../type-aliases/SqliteOptions.md) |

## Returns

`SqliteHandle`

## Example

```ts
// With Prisma schema
  const db = sqlite({ prismaSchema: './prisma/schema' });

  // With raw SQL init
  const db = sqlite({ init: './schema.sql' });

  // Empty database
  const db = sqlite();
```
