<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Function: selected()

```ts
function selected(element): ElementRef;
```

Defined in: [src/core/elements/elements.ts:189](https://github.com/jterrazz/package-test/blob/main/src/core/elements/elements.ts#L189)

Which option the field is on: `see(selected(option('LinkedIn')))`, and
`gone(selected(option('X')))` for the one it left.

A modifier for the same reason as [disabled](disabled.md): being chosen is a state
of an element the vocabulary already names. No matcher answers for it — a
`<option>` is not a checkbox — so both adapters read the `:checked` the CSS
spec gives a selected option.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`ElementRef`](../type-aliases/ElementRef.md) |

## Returns

[`ElementRef`](../type-aliases/ElementRef.md)
