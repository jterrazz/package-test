<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Class: JsonAccessor

Defined in: [core/specification/shared/result/json.ts:19](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/json.ts#L19)

Read-only accessor for a JSON payload parsed from a text stream (stdout).

Lazily parses the JSON on first use; parse errors are deferred until the
value is read so that callers can still read the raw stream text without
triggering a throw.

ANSI escape sequences are stripped by default before `JSON.parse`
(CONVENTIONS D6). When a `transform` is configured on the spec runner, it
runs on the stripped text — an escape hatch for application noise not
covered by the `{{token}}` grammar.

Assertions go through `expect()`: `expect(result.json).toMatch('name.json')`
(deep-equal against `expected/<name>`, with `{{placeholder}}` support).

## Constructors

### Constructor

```ts
new JsonAccessor(
   rawText, 
   testDir, 
   transform?, 
   captures?): JsonAccessor;
```

Defined in: [core/specification/shared/result/json.ts:29](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/json.ts#L29)

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `rawText` | `string` |
| `testDir` | `string` |
| `transform?` | (`text`) => `string` |
| `captures?` | [`CaptureScope`](../interfaces/CaptureScope.md) |

#### Returns

`JsonAccessor`

## Accessors

### value

#### Get Signature

```ts
get value(): unknown;
```

Defined in: [core/specification/shared/result/json.ts:42](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/json.ts#L42)

The parsed JSON value. Throws if the text is not valid JSON.

##### Returns

`unknown`
