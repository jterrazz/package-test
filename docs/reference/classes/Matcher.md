<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Class: Matcher

Defined in: core/matching/match.ts:70

A dynamic-value matcher. Created via the [match](../variables/match.md) factories — never
constructed directly by user code.

## Constructors

### Constructor

```ts
new Matcher(kind, options?): Matcher;
```

Defined in: core/matching/match.ts:81

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `kind` | [`MatcherKind`](../type-aliases/MatcherKind.md) |
| `options` | \{ `includes?`: `string`; `notRef?`: `string`; `refName?`: `string`; `regex?`: `RegExp`; \} |
| `options.includes?` | `string` |
| `options.notRef?` | `string` |
| `options.refName?` | `string` |
| `options.regex?` | `RegExp` |

#### Returns

`Matcher`

## Properties

### includes?

```ts
readonly optional includes?: string;
```

Defined in: core/matching/match.ts:72

For kind 'includes': the substring the actual value must contain.

***

### kind

```ts
readonly kind: MatcherKind;
```

Defined in: core/matching/match.ts:73

***

### notRef?

```ts
readonly optional notRef?: string;
```

Defined in: core/matching/match.ts:75

For kind 'ref': the capture to assert inequality against.

***

### refName?

```ts
readonly optional refName?: string;
```

Defined in: core/matching/match.ts:77

For kind 'ref': the capture name.

***

### regex?

```ts
readonly optional regex?: RegExp;
```

Defined in: core/matching/match.ts:79

For kind 'regex': the pattern the actual value must match.

## Methods

### toJSON()

```ts
toJSON(): string;
```

Defined in: core/matching/match.ts:112

#### Returns

`string`

***

### toString()

```ts
toString(): string;
```

Defined in: core/matching/match.ts:93

Placeholder-style rendering used in failure diffs and serialization.

#### Returns

`string`
