<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Class: CallResult\<Returned\>

Defined in: [src/specification/facets/integration/result.ts:39](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/integration/result.ts#L39)

The result of an in-process call — the integration facet's whole surface.

Two readings, never both: `value` is what the call RETURNED, `error` is
what it THREW. A module that refuses is specified the same way a module
that answers is, and neither needs a `try`/`catch` in the test — which is
the shape that used to make a refusal spec three times the size of the
happy one, and the shape that quietly passes when nothing throws at all.

Both are the package's ordinary accessors, so the golden mechanism reaches
them, and `Returned` is what `.call<T>()` returned — `result.value.value` is
that type, not `unknown`, so a one-field reading needs no golden file: `toMatch('<name>.json'|'<name>.txt')`, the `{{token}}` grammar,
`{ frozen }` and `TEST_UPDATE=1` all work here exactly as on an HTTP
response or a command's stdout.

## Extends

- [`BaseResult`](BaseResult.md)

## Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Returned` | `unknown` |

## Constructors

### Constructor

```ts
new CallResult<Returned>(options): CallResult<Returned>;
```

Defined in: [src/specification/facets/integration/result.ts:42](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/integration/result.ts#L42)

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `options` | `CallResultOptions` |

#### Returns

`CallResult`\<`Returned`\>

#### Overrides

[`BaseResult`](BaseResult.md).[`constructor`](BaseResult.md#constructor)

## Accessors

### error

#### Get Signature

```ts
get error(): TextAccessor;
```

Defined in: [src/specification/facets/integration/result.ts:57](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/integration/result.ts#L57)

What the call THREW, as text — the message of an `Error`, the value
itself when something else was thrown, and empty when it returned.

Empty rather than absent on purpose: `await expect(result.error).toBeEmpty()`
is how a spec says "and it did not refuse" — awaited, because the
matcher answers a promise on every subject it takes, and a dropped one
passes the test while the failure surfaces as an unhandled rejection
(D2 refuses the bare form).

##### Returns

[`TextAccessor`](TextAccessor.md)

***

### value

#### Get Signature

```ts
get value(): CallValue<Returned>;
```

Defined in: [src/specification/facets/integration/result.ts:71](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/integration/result.ts#L71)

What the call RETURNED. A string reads as a stream (`<name>.txt`),
anything else as JSON (`<name>.json`) — the same two subjects every
other facet's results are built from.

A call that threw returns the empty value: what it produced is
[CallResult.error](#error).

##### Returns

`CallValue`\<`Returned`\>

## Methods

### directory()

```ts
directory(path?): DirectoryAccessor;
```

Defined in: [src/specification/facets/\_common/result/result.ts:55](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/result/result.ts#L55)

Access a directory (relative to the working directory) for snapshot assertions.

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `path` | `string` | `'.'` |

#### Returns

[`DirectoryAccessor`](DirectoryAccessor.md)

#### Inherited from

[`BaseResult`](BaseResult.md).[`directory`](BaseResult.md#directory)

***

### file()

```ts
file(path): FileAccessor;
```

Defined in: [src/specification/facets/\_common/result/result.ts:61](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/result/result.ts#L61)

Access a single file (relative to the working directory) for content assertions.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |

#### Returns

[`FileAccessor`](../type-aliases/FileAccessor.md)

#### Inherited from

[`BaseResult`](BaseResult.md).[`file`](BaseResult.md#file)

***

### table()

```ts
table(tableName, options?): TableAccessor;
```

Defined in: [src/specification/facets/\_common/result/result.ts:86](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/result/result.ts#L86)

Access a database table for row-level assertions via expect() matchers.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `tableName` | `string` |
| `options?` | \{ `database?`: `string`; \} |
| `options.database?` | `string` |

#### Returns

[`TableAccessor`](TableAccessor.md)

#### Inherited from

[`BaseResult`](BaseResult.md).[`table`](BaseResult.md#table)
