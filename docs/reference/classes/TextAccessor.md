<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Class: TextAccessor

Defined in: [core/specification/shared/result/text.ts:36](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L36)

Read-only accessor for a captured text handle — THE universal one: stdout,
stderr, container logs, and file text all surface as a `TextAccessor`.

Backed by a primitive string, exposed via [text](../functions/text.md), but also provides
`toString()` / `valueOf()` for string coercion, so common patterns like
`String(result.stdout)` and template-literal interpolation still work.

ANSI escape sequences are stripped by default before every comparison
(CONVENTIONS D6) — `.text` stays raw. The runner `transform` option remains
an escape hatch for application noise not covered by the `{{token}}`
grammar.

Text operations are **closed** over the type: `.grep(pattern)` returns a
`TextAccessor` (not a bare string), preserving the `expected/`-resolution
context and the `transform`, so results are chainable
(`result.stdout.grep(a).grep(b)`) and snapshot-able
(`expect(result.stdout.grep('users.ts')).toMatch('block.txt')`).

Assertions go through `expect()` matchers: `expect(result.stdout).toContain(...)`
and `expect(result.stdout).toMatch('name.txt')` (resolved against
`expected/<name>`, flat — with `{{token}}` support, CONVENTIONS D4).

## Constructors

### Constructor

```ts
new TextAccessor(
   value, 
   streamName, 
   testDir, 
   options?): TextAccessor;
```

Defined in: [core/specification/shared/result/text.ts:48](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L48)

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `string` |
| `streamName` | `string` |
| `testDir` | `string` |
| `options` | \{ `captures?`: [`CaptureScope`](../interfaces/CaptureScope.md); `transform?`: (`text`) => `string`; \} |
| `options.captures?` | [`CaptureScope`](../interfaces/CaptureScope.md) |
| `options.transform?` | (`text`) => `string` |

#### Returns

`TextAccessor`

## Properties

### text

```ts
readonly text: string;
```

Defined in: [core/specification/shared/result/text.ts:44](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L44)

The raw captured text (never transformed, ANSI preserved).

## Methods

### grep()

```ts
grep(pattern): TextAccessor;
```

Defined in: [core/specification/shared/result/text.ts:74](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L74)

Keep only the blank-line-separated blocks of the text that contain
`pattern` (how linter/compiler output is structured), returned as a new
`TextAccessor` — the same subject type, so the result is chainable and
snapshot-able. The scalpel for probing large tool outputs; snapshot the
whole surface by default, reach for `.grep()` for targeted checks.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pattern` | `string` |

#### Returns

`TextAccessor`

***

### toString()

```ts
toString(): string;
```

Defined in: [core/specification/shared/result/text.ts:81](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L81)

#### Returns

`string`

***

### valueOf()

```ts
valueOf(): string;
```

Defined in: [core/specification/shared/result/text.ts:85](https://github.com/jterrazz/package-test/blob/main/src/core/specification/shared/result/text.ts#L85)

#### Returns

`string`
