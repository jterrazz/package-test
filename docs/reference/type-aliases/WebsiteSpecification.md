<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Type Alias: WebsiteSpecification

```ts
type WebsiteSpecification = object;
```

Defined in: [src/specification/facets/\_common/builder.ts:317](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L317)

The `website` facet — page chain entry handed out by
`specification.website()`. Setup methods chain; action methods are
terminal. `.visit()` renders the page in the shared browser; `.fetch()`
performs one raw HTTP exchange and never follows redirects.

## Properties

### clock

```ts
clock: (iso) => WebsiteSpecification;
```

Defined in: [src/specification/facets/\_common/builder.ts:322](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L322)

Pin the PAGE's `Date` at `iso` for this chain — what the site's own
scripts read. A `.fetch()` opens no page and refuses the setup.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `iso` | `string` |

#### Returns

`WebsiteSpecification`

***

### fetch

```ts
fetch: (path) => Promise<FetchResult>;
```

Defined in: [src/specification/facets/\_common/builder.ts:332](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L332)

Perform one raw HTTP GET — redirects surface as 3xx results, never followed.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |

#### Returns

`Promise`\<[`FetchResult`](../interfaces/FetchResult.md)\>

***

### headers

```ts
headers: (headers) => WebsiteSpecification;
```

Defined in: [src/specification/facets/\_common/builder.ts:324](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L324)

Set HTTP headers for the exchange (incl. User-Agent overrides). Multiple calls merge.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `headers` | `Record`\<`string`, `string`\> |

#### Returns

`WebsiteSpecification`

***

### intercept

```ts
intercept: InterceptMethod<WebsiteSpecification>;
```

Defined in: [src/specification/facets/\_common/builder.ts:329](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L329)

Declare the chain's backend contracts — served by the declared stub
backend (requires the runner's `backend` option). Multiple calls append.

***

### visit

```ts
visit: (path, scenario?) => Promise<PageResult>;
```

Defined in: [src/specification/facets/\_common/builder.ts:338](https://github.com/jterrazz/package-test/blob/main/src/specification/facets/_common/builder.ts#L338)

Render the page in the shared browser and resolve with the captured
document. With a scenario, the visitor interacts first (the When) and
the capture reflects the FINAL page state.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `path` | `string` |
| `scenario?` | [`VisitScenario`](VisitScenario.md) |

#### Returns

`Promise`\<[`PageResult`](../interfaces/PageResult.md)\>
