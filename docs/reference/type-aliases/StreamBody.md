<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Type Alias: StreamBody

```ts
type StreamBody = object;
```

Defined in: [src/model/contracts/types.ts:84](https://github.com/jterrazz/package-test/blob/main/src/model/contracts/types.ts#L84)

A body that arrives in pieces — what `http.stream()` and `http.sse()` build.

The chunks are written in order, `delayBetweenChunks` milliseconds apart,
and the connection closes after the last one. It is the chunks and their
ORDER that a streaming subject is specified against: a client that renders
tokens as they land, or one that reconnects on a half-read body.

## Properties

### chunks

```ts
chunks: readonly string[];
```

Defined in: [src/model/contracts/types.ts:86](https://github.com/jterrazz/package-test/blob/main/src/model/contracts/types.ts#L86)

The pieces, written in order.

***

### contentType

```ts
contentType: string;
```

Defined in: [src/model/contracts/types.ts:88](https://github.com/jterrazz/package-test/blob/main/src/model/contracts/types.ts#L88)

The `content-type` the stream is served under.

***

### delayBetweenChunks

```ts
delayBetweenChunks: number;
```

Defined in: [src/model/contracts/types.ts:90](https://github.com/jterrazz/package-test/blob/main/src/model/contracts/types.ts#L90)

Milliseconds between two chunks. Default 0 — everything at once.

***

### kind

```ts
kind: typeof STREAM_BODY;
```

Defined in: [src/model/contracts/types.ts:91](https://github.com/jterrazz/package-test/blob/main/src/model/contracts/types.ts#L91)
