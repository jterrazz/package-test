<!-- GENERATED by `typescript docs` — DO NOT EDIT -->
# Type Alias: SpecificationConfig

```ts
type SpecificationConfig = object;
```

Defined in: [src/model/chain/builder.ts:54](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L54)

Adapter configuration passed to the specification facets at setup time.

## Properties

### backend?

```ts
optional backend?: StubBackend;
```

Defined in: [src/model/chain/builder.ts:59](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L59)

The declared stub backend (website/mobile facets) — armed with the
chain's contracts before every terminal action.

***

### backendUrl?

```ts
optional backendUrl?: string;
```

Defined in: [src/model/chain/builder.ts:64](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L64)

Base URL of the running stub backend — allow-listed through the
browser's `external: 'block'` policy so client-side fetches reach it.

***

### baseUrl?

```ts
optional baseUrl?: string;
```

Defined in: [src/model/chain/builder.ts:66](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L66)

Base URL of the website under test (website facet only).

***

### browser?

```ts
optional browser?: () => Promise<BrowserPort>;
```

Defined in: [src/model/chain/builder.ts:72](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L72)

Lazy browser accessor (website facet only). The first `.visit()`
launches the shared browser instance; `.fetch()`-only spec files never
pay the browser cost.

#### Returns

`Promise`\<[`BrowserPort`](BrowserPort.md)\>

***

### bundleId?

```ts
optional bundleId?: string;
```

Defined in: [src/model/chain/builder.ts:80](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L80)

Bundle id of the app under test (mobile facet only) — the app `.open()` relaunches.

***

### command?

```ts
optional command?: CliPort;
```

Defined in: [src/model/chain/builder.ts:81](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L81)

***

### database?

```ts
optional database?: DatabasePort;
```

Defined in: [src/model/chain/builder.ts:82](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L82)

***

### databaseKeys?

```ts
optional databaseKeys?: string[];
```

Defined in: [src/model/chain/builder.ts:88](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L88)

Keys of the declared services record that are databases. Drives the
CONVENTIONS A7 rule: with 2+ databases the `database` option is
mandatory on `.seed()` / `.table()`; with exactly one it is forbidden.

***

### databases?

```ts
optional databases?: Map<string, DatabasePort>;
```

Defined in: [src/model/chain/builder.ts:89](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L89)

***

### defaultEnv?

```ts
optional defaultEnv?: CliEnv;
```

Defined in: [src/model/chain/builder.ts:110](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L110)

Environment applied to every run of the declared binary
(`specification.cli({ defaults })`) — under a chain's own `.env()` and
under a document's `env:`, both of which win.

***

### device?

```ts
optional device?: () => Promise<DevicePort>;
```

Defined in: [src/model/chain/builder.ts:96](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L96)

Lazy device accessor (mobile facet only). The first `.open()` creates
the shared driver session; the appium/webdriverio integration stays a
lazy import so the optional peer is only loaded when a spec opens the
app.

#### Returns

`Promise`\<[`DevicePort`](DevicePort.md)\>

***

### dockerConfig?

```ts
optional dockerConfig?: DockerSpecConfig;
```

Defined in: [src/model/chain/builder.ts:97](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L97)

***

### dockerTestRunId?

```ts
optional dockerTestRunId?: string;
```

Defined in: [src/model/chain/builder.ts:104](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L104)

Unique id shared by every spec from this runner instance.
Stable for the runner's lifetime so multi-step tests (spawn in
one run, inspect in another) see the same container scope. The
facet factories auto-populate this when `dockerConfig` is present.

***

### envSets?

```ts
optional envSets?: Record<string, CliEnv>;
```

Defined in: [src/model/chain/builder.ts:115](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L115)

Named environment SETS a spec document may name by bare word
(`env: frozen`). Declared once per app in `specification.cli()`.

***

### external?

```ts
optional external?: "allow" | "block";
```

Defined in: [src/model/chain/builder.ts:78](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L78)

Cross-origin request policy for visits (website facet only): `'block'`
aborts requests leaving the site under test — the browser-side analog
of strict intercepts.

***

### jobs?

```ts
optional jobs?: JobHandle[];
```

Defined in: [src/model/chain/builder.ts:116](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L116)

***

### root?

```ts
optional root?: string;
```

Defined in: [src/model/chain/builder.ts:118](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L118)

The project root — the working directory a document's `serve:` command runs from.

***

### server?

```ts
optional server?: ServerPort;
```

Defined in: [src/model/chain/builder.ts:119](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L119)

***

### serveRegistry?

```ts
optional serveRegistry?: Record<string, LiterateServeRegistration>;
```

Defined in: [src/model/chain/builder.ts:124](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L124)

Named servers a spec document may start (`serve: [mcp]`). Declared once
per app in `specification.cli()`.

***

### services?

```ts
optional services?: Record<string, ServiceHandle>;
```

Defined in: [src/model/chain/builder.ts:131](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L131)

The declared services record. In cli mode, drives the automatic
connection-URL injection into the child env (CONVENTIONS B6):
`<KEY>_URL` per service, plus `DATABASE_URL` / `REDIS_URL` when
unambiguous.

***

### transform?

```ts
optional transform?: (text) => string;
```

Defined in: [src/model/chain/builder.ts:136](https://github.com/jterrazz/package-test/blob/main/src/model/chain/builder.ts#L136)

Optional normaliser applied to command stdout/stderr before every
comparison. Does not mutate the raw `.text` accessor.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `text` | `string` |

#### Returns

`string`
