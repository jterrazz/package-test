<!-- GENERATED by 'npm run docs' — DO NOT EDIT -->

# `@jterrazz/test` — Module tests

> Generated from `src/lint/cards.ts` and the facet declaration by `npm run docs`. The chapter is [05-module-tests.md](../../../docs/05-module-tests.md).

**Specifies** — Given these inputs and these ports, what does this module return, and what does it ask of its collaborators?

**Lives at** — `<file>.test.ts` beside `<file>.ts` — there is no facet folder

**Collected by** — `unit()` in `vitest.config.ts`

**Written as** — Nothing is started: a plain `test()` beside the module, reaching for the primitives below.

**Not this kind** — A golden, a real service, a DOM, a data asset, `vi.mock` outside the allow-list, `vi.stubGlobal` — each has a kind that owns it.

## Goldens

None here: a module test may not carry a golden (rule D20) — a module whose answer deserves one is an integration spec.

## Primitives

| Name          | Written                                                  | Does                                                                                                                    |
| ------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `clock()`     | `using _ = clock.at('2026-03-04T09:30:00Z')`             | Pins `Date` (`clock.at`) or the scheduler too (`clock.run` + `clock.advance`), for the scope                            |
| `intercept()` | `await using _ = await intercept(contracts, { origin })` | The chain-less double: declares the network for the scope. `{ origin }` states what a relative request resolves against |
| `mockOf()`    | `mockOf<Port>({ deep })`                                 | A typed double for an INJECTED port — deep by default, `{ deep: false }` for the flat proxy                             |
| `match.*`     | `expect(value).toStrictEqual({ id: match.uuid() })`      | The token vocabulary in code — the same families a golden writes as `{{token}}`                                         |

## Read next

The chapter: [05-module-tests.md](../../../docs/05-module-tests.md). The fork: [fork.md](fork.md). What the package proves on each capability: [matrix.md](matrix.md). The rules that judge this file: [rules.md](rules.md).
