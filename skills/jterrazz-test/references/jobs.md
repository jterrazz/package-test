<!-- GENERATED by 'npm run docs' — DO NOT EDIT -->

# `@jterrazz/test` — Jobs specs

> Generated from `src/lint/cards.ts` and the facet declaration by `npm run docs`. The chapter is [11-jobs.md](../../../docs/11-jobs.md).

**Specifies** — When this name is triggered, what does the pipeline do to the world it was handed?

**Lives at** — `specs/jobs/<domain>/<aspect>.spec.ts`

**Collected by** — `jobs()` in `vitest.config.ts`

**Constructed with**

```typescript
import { specification } from '@jterrazz/test';

export const { cleanup, jobs } = await specification.jobs({
    jobs: [{ execute: async () => {}, name: 'daily-digest' }],
});
```

**Not this kind** — A pure function inside the pipeline is a module test; the pipeline met through its name is this.

## Options

| Name       | Written                          | Does                                                                               |
| ---------- | -------------------------------- | ---------------------------------------------------------------------------------- |
| `services` | `services: { main: postgres() }` | Named infrastructure the chain gets, isolated per worker and reset per chain       |
| `root`     | `root: './apps/api'`             | Project-root override (rule A9); auto-discovered from the calling file when absent |
| `jobs`     | `jobs: JobsRegistry`             | The pipeline registry the trigger name is looked up in                             |

## Setups (chainable)

| Name           | Written                          | Does                                                                                    |
| -------------- | -------------------------------- | --------------------------------------------------------------------------------------- |
| `.clock()`     | `.clock('2026-03-04T09:30:00Z')` | Pins `Date` for this chain — the one primitive for time                                 |
| `.intercept()` | `.intercept(contracts)`          | Declares what the network answers, from contracts — strict from the first one (rule D7) |
| `.seed()`      | `.seed('users.sql')`             | Loads a `_seeds/*.sql` file into the named service before the action                    |

## Terminal actions (exactly one)

| Name         | Written                    | Does                                                  |
| ------------ | -------------------------- | ----------------------------------------------------- |
| `.trigger()` | `.trigger('daily-digest')` | Triggers the job by NAME, the way the scheduler would |

## The result

| Name       | Written                                 | Does                                                                           |
| ---------- | --------------------------------------- | ------------------------------------------------------------------------------ |
| `.table()` | `.table('users', { database: 'main' })` | The rows a service holds after the action — the oracle of anything that writes |

## Goldens

| Name                | Written                                                        | Does                                                                                                         |
| ------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `toMatch('<name>')` | `expect(subject).toMatch('<name>.<ext>')`                      | Compares the subject to the file of that name under `_expected/`, tokens resolved; `TEST_UPDATE=1` writes it |
| `toMatchRows()`     | `expect(result.table('users')).toMatchRows({ columns, rows })` | Compares a table to a column list and one array of cells per row                                             |

## Read next

The chapter: [11-jobs.md](../../../docs/11-jobs.md). The fork: [fork.md](fork.md). What the package proves on each capability: [matrix.md](matrix.md). The rules that judge this file: [rules.md](rules.md).
